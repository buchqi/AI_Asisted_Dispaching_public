import type { NextConfig } from "next";

// Central Next.js configuration.
// Keep app-wide framework options here instead of scattering them across files.
const nextConfig: NextConfig = {
  // React Strict Mode helps catch unsafe patterns during development.
  reactStrictMode: true
};

export default nextConfig;
