# Components Structure

Reusable, domain-free UI lives here.

- `components/layout` owns persistent app chrome.
- `components/ui` owns small reusable primitives and UI helpers.

Business screens live in `features/`, route wrappers live in `app/`, backend
request boundaries live in `api/`, and shared contracts live in `types/`.
