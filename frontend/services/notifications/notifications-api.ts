// notificationsApi is reserved for backend notification persistence.
// Current notifications live in Zustand because the app is still frontend-only.
export const notificationsApi = {
  list() {
    return [];
  }
};
