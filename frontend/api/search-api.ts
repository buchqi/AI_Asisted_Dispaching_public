import { SearchRequest, SearchResult } from "@/types/search";

export const searchApi = {
  search(_request: SearchRequest): SearchResult[] {
    // TODO: Replace with FastAPI-backed search.
    return [];
  }
};
